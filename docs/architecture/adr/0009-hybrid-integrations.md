# ADR 0009: hybrid integrations

**Status:** accepted.

Use native persisted data first, then live/cooperative public contracts. Never scrape rendered output or import private APIs. Each metric carries evidence confidence and unsupported/version state.
