# ADR 0008: harness adapter

**Status:** accepted.

Implement Pi adapter only in v1. Canonical records remain harness-neutral so Hermes can map later without making v1 depend on Hermes or its Python/plugin runtime.
