# ADR 0006: branch scope semantics

**Status:** accepted.

Current view defaults to active ancestry after tracking marker. Resource/history views default to all tree entries after marker. A branch is leaf/path selection, not invented native identity.
