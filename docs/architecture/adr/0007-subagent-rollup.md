# ADR 0007: subagent roll-up

**Status:** accepted.

Use public pi-subagents artifacts/results for attribution. Child usage breaks down parent totals and is never added again. Missing causal evidence is unavailable, not inferred from time.
